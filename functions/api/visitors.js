const headers={"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store","X-Content-Type-Options":"nosniff","Referrer-Policy":"strict-origin-when-cross-origin"};
const json=(payload,status=200)=>new Response(JSON.stringify(payload),{status,headers});
function cleanVisitorId(value){const id=String(value||"").trim();return /^[A-Za-z0-9-]{16,80}$/.test(id)?id:"";}
function managerConfig(env){const base=String(env.RYVEX_BOT_API_URL||"").replace(/\/$/,"");const key=String((env.RYVEX_WEBSITE_API_KEY||env.RYVEX_BOT_API_KEY)||"");return {base,key,configured:/^https:\/\//i.test(base)&&!!key};}
async function managerVisit(env,method,visitorId="") {const {base,key,configured}=managerConfig(env);if(!configured)return null;try{const response=await fetch(`${base}/api/v1/site-visit?platform=website`,{method,headers:{"Accept":"application/json","Content-Type":"application/json","X-RYVEX-API-KEY":key,"X-RYVEX-PLATFORM":"website","X-RYVEX-PLATFORM-VERSION":"2.5.1"},body:method==="POST"?JSON.stringify({visitorId,platform:"website"}):undefined,cf:{cacheEverything:false}});const body=await response.json().catch(()=>null);return response.ok&&body?.ok!==false?body:null}catch{return null}}
function fallbackMetrics(count){return {platform:"website",unique:Number(count)||0,todayUnique:null,active24h:null,pageViews:null,todayViews:null,privacy:"anonymous_random_id"};}
export async function onRequestGet({env}){const manager=await managerVisit(env,"GET");if(manager)return json({ok:true,configured:true,backend:"ryvex-manager",...manager});const store=env.RYVEX_VISITORS;if(store&&typeof store.get==="function"){const count=Number(await store.get("site:unique-visitors"))||0;return json({ok:true,configured:true,backend:"cloudflare-kv",metrics:fallbackMetrics(count)});}return json({ok:true,configured:false,metrics:null});}
export async function onRequestPost({request,env}){let body={};try{body=await request.json()}catch{return json({ok:false,error:"invalid_json"},400)}const visitorId=cleanVisitorId(body.visitorId);if(!visitorId)return json({ok:false,error:"invalid_visitor_id"},400);const manager=await managerVisit(env,"POST",visitorId);if(manager)return json({ok:true,configured:true,backend:"ryvex-manager",...manager});const store=env.RYVEX_VISITORS;if(store&&typeof store.get==="function"&&typeof store.put==="function"){const visitorKey=`site:visitor:${visitorId}`,alreadySeen=await store.get(visitorKey);let count=Number(await store.get("site:unique-visitors"))||0;if(!alreadySeen){count+=1;await Promise.all([store.put(visitorKey,"1"),store.put("site:unique-visitors",String(count))])}return json({ok:true,configured:true,backend:"cloudflare-kv",metrics:fallbackMetrics(count)});}return json({ok:true,configured:false,metrics:null});}
